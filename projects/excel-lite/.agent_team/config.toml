# Generated by agent-team init. Commit this file; keep API keys and tokens in .env.
#
# Template profile: "slim". The slim profile is the external-consumer starter; full adds the self-dogfood topology.
# PM provider: "linear".
# Required provider keys now:
# - linear.team_id: Linear team UUID. Required only when pm.provider is linear.
# - linear.ticket_prefix: Linear ticket prefix, e.g. SQU. Required only when pm.provider is linear.
# Provider sections for modes you are not using can stay blank.
# Optional runtime, health, notification, label, and project fields can stay at defaults until needed.
# After editing provider settings, run: agent-team doctor --commands

[github]
  agent_column = "Ready for Agent"
  agent_id = ""
  agent_login = ""
  attention_column = "Todo"
  attention_label = ""
  attention_state = "open"
  in_progress_column = "In Progress"
  in_progress_label = ""
  in_progress_state = "open"
  labels = []
  owner = ""
  project_number = 0
  project_owner = ""
  project_status_field = "Status"
  repo = ""

[health]
  job_stale_after = "24h"
  status_stale_after = "10m"
  terminal_retention = "0"

[linear]
  agent_column = "Ready for Agent"
  agent_user_id = ""
  attention_state = ""
  in_progress_state = "In Progress"
  labels = []
  team_id = "00000000-0000-0000-0000-000000000000"
  ticket_prefix = "XL"

[notifications]
  idle_renotify = "0"
  phase_transitions = ["blocked"]

[otel]
  enabled = false
  endpoint = ""

[pm]
  provider = "none"

[project]
  id = "6c7bed9e-9e7e-43cf-a2ca-1b67c38f2cce"
  parent_uri = ""

[skills]
  team = []

[team]
  pm_tool = "none"

[template]
  profile = "slim"

[runtime]
  kind = "codex"
  binary = "scripts/codex-chatgpt-only.sh"
