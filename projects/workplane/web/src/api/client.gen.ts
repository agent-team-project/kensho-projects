// Code generated by scripts/generate.py from contracts/openapi.yaml; DO NOT EDIT.
// Contract SHA-256: 4075807ba50c01fc6517cd0694d510a361a71d1c11d0643237fed82cc3716fd0

export type LoginRequest = {
  email: string;
  password: string;
};

export type CreateExplorationProject = {
  title: string;
  outcome: string;
  hypothesis: string;
  falsifier: string;
  decision_criteria: Array<string>;
  experiment_bound: string;
};

export type RecordDecision = {
  kind: "continue";
  question: string;
  choice: string;
  alternatives: Array<string>;
  rationale: string;
  evidence: Array<string>;
  consequences: Array<string>;
};

export type LifecycleReasonRequest = {
  reason: string;
};

export type DeliverableInput = {
  title: string;
  description: string;
  required: boolean;
  weight: number;
  state: "draft" | "ready";
  acceptance_criteria: Array<string>;
};

export type PromoteProjectRequest = {
  decision: PromotionDecision;
  residual_uncertainty: string;
  priority_rationale: string;
  deliverables: Array<DeliverableInput>;
};

export type ForecastInput = {
  p50_at: string;
  p90_at: string;
  review_after: string;
  basis: string;
  assumptions: Array<string>;
  reason_codes: Array<"scope-change" | "new-evidence" | "dependency-change" | "capacity-change" | "quality-finding" | "incident" | "estimate-correction" | "hold-change" | "other">;
  impact: string;
};

export type TargetInput = {
  target_at: string;
  reason: string;
};

export type DeadlineInput = {
  deadline_at: string;
  source: "contract" | "launch-window" | "demonstration" | "regulation" | "other";
  description: string;
};

export type EvidenceInput = {
  kind: "report" | "test-run" | "link" | "image" | "observation" | "measurement";
  title: string;
  claim: string;
  source: string;
  content?: string;
  uri?: string;
  metadata: Record<string, string>;
  supports: Array<EvidenceSupportInput>;
};

export type GateInput = {
  name: string;
  kind: "automated" | "review" | "approval";
  hard: boolean;
  independence_required: boolean;
  required_evidence: Array<EvidenceRequirementInput>;
};

export type VerdictInput = {
  result: "pass" | "fail";
  evidence_ids: Array<string>;
  findings: Array<FindingInput>;
};

export type FindingDispositionInput = {
  evidence_ids: Array<string>;
  rationale: string;
};

export type SubmissionInput = {
  evidence_ids: Array<string>;
  note: string;
};

export type BounceInput = {
  finding_id: string;
  rationale: string;
};

export type ReviewNoteInput = {
  note: string;
};

export type WaiverInput = {
  question: string;
  choice: string;
  alternatives: Array<string>;
  rationale: string;
  evidence_ids: Array<string>;
  consequences: Array<string>;
  residual_risk: string;
};

export type PromotionDecision = {
  question: string;
  choice: string;
  alternatives: Array<string>;
  rationale: string;
  evidence: Array<string>;
  consequences: Array<string>;
};

export type EvidenceSupportInput = {
  target_type: "deliverable" | "gate" | "finding" | "decision";
  target_id: string;
};

export type EvidenceRequirementInput = {
  kind: "report" | "test-run" | "link" | "image" | "observation" | "measurement";
  claim: string;
};

export type FindingInput = {
  title: string;
  detail: string;
  blocking: boolean;
};

export type Session = {
  actor_id: string;
  actor_kind: "human";
  expires_at: string;
  csrf_token: string;
};

export type Project = {
  id: string;
  organization_id: string;
  title: string;
  outcome: string;
  mode: "exploration" | "exploitation";
  state: "proposed" | "active" | "held" | "completed" | "stopped" | "cancelled";
  version: number;
  hypothesis: string;
  falsifier: string;
  decision_criteria: Array<string>;
  experiment_bound: string;
};

export type Decision = {
  id: string;
  project_id: string;
  actor_id: string;
  actor_kind: "human" | "agent";
  principal_id: string | null;
  recorded_at: string;
  kind: "continue" | "promote" | "waiver";
  question: string;
  choice: string;
  alternatives: Array<string>;
  rationale: string;
  evidence: Array<string>;
  consequences: Array<string>;
};

export type Deliverable = {
  id: string;
  organization_id: string;
  project_id: string;
  title: string;
  description: string;
  required: boolean;
  weight: number;
  state: "draft" | "ready" | "submitted" | "bounced" | "accepted" | "waived" | "cancelled";
  acceptance_criteria: Array<string>;
  version: number;
  waiver_decision_id: string | null;
  created_by: string;
  created_at: string;
  updated_at: string;
};

export type EvidenceSupport = {
  target_type: "deliverable" | "gate" | "finding" | "decision";
  target_id: string;
};

export type Evidence = {
  id: string;
  organization_id: string;
  project_id: string;
  kind: "report" | "test-run" | "link" | "image" | "observation" | "measurement";
  title: string;
  claim: string;
  source: string;
  content: string | null;
  uri: string | null;
  metadata: Record<string, string>;
  integrity_digest: string;
  produced_by: string;
  producer_kind: "human" | "agent";
  principal_id: string | null;
  produced_at: string;
  supersedes_id: string | null;
  supports: Array<EvidenceSupport>;
};

export type Gate = {
  id: string;
  organization_id: string;
  project_id: string;
  deliverable_id: string;
  name: string;
  kind: "automated" | "review" | "approval";
  hard: boolean;
  independence_required: boolean;
  state: "pending" | "passed" | "failed" | "waived";
  version: number;
  waiver_decision_id: string | null;
  required_evidence: Array<EvidenceRequirementInput>;
  created_by: string;
  created_at: string;
  updated_at: string;
};

export type Verdict = {
  id: string;
  organization_id: string;
  project_id: string;
  deliverable_id: string;
  gate_id: string;
  result: "pass" | "fail";
  evidence_ids: Array<string>;
  reviewer_id: string;
  reviewer_kind: "human" | "agent";
  principal_id: string | null;
  supersedes_id: string | null;
  created_at: string;
};

export type Finding = {
  id: string;
  organization_id: string;
  project_id: string;
  deliverable_id: string;
  gate_id: string;
  verdict_id: string;
  title: string;
  detail: string;
  blocking: boolean;
  state: "open" | "resolved" | "withdrawn";
  version: number;
  created_by: string;
  created_at: string;
  updated_at: string;
};

export type FindingAction = {
  id: string;
  organization_id: string;
  project_id: string;
  deliverable_id: string;
  finding_id: string;
  action: "resolve" | "withdraw";
  evidence_ids: Array<string>;
  rationale: string;
  actor_id: string;
  actor_kind: "human" | "agent";
  principal_id: string | null;
  created_at: string;
};

export type Submission = {
  id: string;
  organization_id: string;
  project_id: string;
  deliverable_id: string;
  kind: "submit" | "resubmit";
  evidence_ids: Array<string>;
  note: string;
  submitted_by: string;
  submitter_kind: "human" | "agent";
  principal_id: string | null;
  created_at: string;
};

export type VerdictResult = {
  gate: Gate;
  verdict: Verdict;
  findings: Array<Finding>;
};

export type FindingActionResult = {
  finding: Finding;
  action: FindingAction;
};

export type SubmissionResult = {
  deliverable: Deliverable;
  submission: Submission;
};

export type DeliverableWaiverResult = {
  deliverable: Deliverable;
  decision: Decision;
};

export type GateWaiverResult = {
  gate: Gate;
  decision: Decision;
};

export type Forecast = {
  id: string;
  organization_id: string;
  project_id: string;
  deliverable_id?: string | null;
  scope: "project" | "deliverable";
  p50_at: string;
  p90_at: string;
  review_after: string;
  basis: string;
  assumptions: Array<string>;
  reason_codes: Array<string>;
  impact: string;
  supersedes_id?: string | null;
  created_by: string;
  created_at: string;
  current: boolean;
  stale: boolean;
  attention_only: boolean;
};

export type Target = {
  id: string;
  organization_id: string;
  project_id: string;
  target_at: string;
  reason: string;
  supersedes_id?: string | null;
  created_by: string;
  created_at: string;
  current: boolean;
  missed: boolean;
  attention_only: boolean;
};

export type Deadline = {
  id: string;
  organization_id: string;
  project_id: string;
  deadline_at: string;
  source: "contract" | "launch-window" | "demonstration" | "regulation" | "other";
  description: string;
  supersedes_id?: string | null;
  created_by: string;
  created_at: string;
  current: boolean;
  passed: boolean;
  attention_only: boolean;
};

export type PromotionResult = {
  project: Project;
  decision: Decision;
  deliverables: Array<Deliverable>;
};

export type Activity = {
  event_id: string;
  event_type: string;
  actor_id: string;
  actor_kind: "human" | "agent";
  principal_id: string | null;
  aggregate_version: number;
  command_id: string;
  request_id: string;
  occurred_at: string;
};

export type Problem = {
  type: string;
  title: string;
  status: number;
  code: string;
  request_id: string;
  detail?: string;
};

export type HumanSessionSecurity = {
  kind: "human-session";
  csrfToken?: string;
};

export type HumanMutationSecurity = {
  kind: "human-session";
  csrfToken: string;
};

export type AgentBearerSecurity = {
  kind: "agent-bearer";
  bearerToken: string;
};

export type RequestSecurity = HumanSessionSecurity | AgentBearerSecurity;

export type RequestOptions = {
  headers?: Record<string, string>;
  security?: RequestSecurity;
};

export type AuthenticatedOptions = RequestOptions;
export type VersionETag = `"${number}"`;
export type MutationOptions = Omit<RequestOptions, "security"> & {
  idempotencyKey: string;
  security: HumanMutationSecurity | AgentBearerSecurity;
};
export type VersionedMutationOptions = MutationOptions & {
  expectedVersion: VersionETag;
};
export type VersionedResponse<T> = {
  body: T;
  version: VersionETag;
};

type TransportResponse = {
  body: unknown;
  headers: Headers;
};

type RuntimeSchema = {
  $ref?: string;
  const?: unknown;
  format?: string;
  type?: string | ReadonlyArray<string>;
  required?: ReadonlyArray<string>;
  additionalProperties?: boolean | RuntimeSchema;
  properties?: Readonly<Record<string, RuntimeSchema>>;
  items?: RuntimeSchema;
  enum?: ReadonlyArray<unknown>;
  pattern?: string;
  minLength?: number;
  maxLength?: number;
  minItems?: number;
  maxItems?: number;
  maxProperties?: number;
  minimum?: number;
  maximum?: number;
  uniqueItems?: boolean;
};

const versionETagPattern = /^"[1-9][0-9]*"$/;
const contractOwnedRequestHeaders = new Set([
  "authorization",
  "idempotency-key",
  "if-match",
  "x-csrf-token"
]);
export const requestBodyMaxBytes = 65536;
export const requestContractSchemas = {
  "LoginRequest": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "email",
      "password"
    ],
    "properties": {
      "email": {
        "type": "string",
        "format": "email",
        "minLength": 1,
        "maxLength": 320
      },
      "password": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1024
      }
    }
  },
  "CreateExplorationProject": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "title",
      "outcome",
      "hypothesis",
      "falsifier",
      "decision_criteria",
      "experiment_bound"
    ],
    "properties": {
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "outcome": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "hypothesis": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "falsifier": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "decision_criteria": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500,
          "pattern": "\\S"
        }
      },
      "experiment_bound": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1000,
        "pattern": "\\S"
      }
    }
  },
  "RecordDecision": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "kind",
      "question",
      "choice",
      "alternatives",
      "rationale",
      "evidence",
      "consequences"
    ],
    "properties": {
      "kind": {
        "type": "string",
        "enum": [
          "continue"
        ]
      },
      "question": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "choice": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "alternatives": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 1000
        }
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "evidence": {
        "type": "array",
        "maxItems": 64,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      },
      "consequences": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      }
    }
  },
  "LifecycleReasonRequest": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "reason"
    ],
    "properties": {
      "reason": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "DeliverableInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "title",
      "description",
      "required",
      "weight",
      "state",
      "acceptance_criteria"
    ],
    "properties": {
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "description": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "required": {
        "type": "boolean"
      },
      "weight": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1000
      },
      "state": {
        "type": "string",
        "enum": [
          "draft",
          "ready"
        ]
      },
      "acceptance_criteria": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000,
          "pattern": "\\S"
        }
      }
    }
  },
  "PromoteProjectRequest": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "decision",
      "residual_uncertainty",
      "priority_rationale",
      "deliverables"
    ],
    "properties": {
      "decision": {
        "$ref": "#/components/schemas/PromotionDecision"
      },
      "residual_uncertainty": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "priority_rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "deliverables": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "$ref": "#/components/schemas/DeliverableInput"
        }
      }
    }
  },
  "ForecastInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "p50_at",
      "p90_at",
      "review_after",
      "basis",
      "assumptions",
      "reason_codes",
      "impact"
    ],
    "properties": {
      "p50_at": {
        "type": "string",
        "format": "date-time"
      },
      "p90_at": {
        "type": "string",
        "format": "date-time"
      },
      "review_after": {
        "type": "string",
        "format": "date-time"
      },
      "basis": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "assumptions": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000,
          "pattern": "\\S"
        }
      },
      "reason_codes": {
        "type": "array",
        "minItems": 1,
        "maxItems": 9,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "enum": [
            "scope-change",
            "new-evidence",
            "dependency-change",
            "capacity-change",
            "quality-finding",
            "incident",
            "estimate-correction",
            "hold-change",
            "other"
          ]
        }
      },
      "impact": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "TargetInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "target_at",
      "reason"
    ],
    "properties": {
      "target_at": {
        "type": "string",
        "format": "date-time"
      },
      "reason": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "DeadlineInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "deadline_at",
      "source",
      "description"
    ],
    "properties": {
      "deadline_at": {
        "type": "string",
        "format": "date-time"
      },
      "source": {
        "type": "string",
        "enum": [
          "contract",
          "launch-window",
          "demonstration",
          "regulation",
          "other"
        ]
      },
      "description": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "EvidenceInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "kind",
      "title",
      "claim",
      "source",
      "metadata",
      "supports"
    ],
    "properties": {
      "kind": {
        "type": "string",
        "enum": [
          "report",
          "test-run",
          "link",
          "image",
          "observation",
          "measurement"
        ]
      },
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "claim": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "source": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "content": {
        "type": "string",
        "minLength": 1,
        "maxLength": 16000,
        "pattern": "\\S"
      },
      "uri": {
        "type": "string",
        "format": "uri-reference",
        "minLength": 1,
        "maxLength": 2000
      },
      "metadata": {
        "type": "object",
        "maxProperties": 32,
        "additionalProperties": {
          "type": "string",
          "minLength": 1,
          "maxLength": 2000
        }
      },
      "supports": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "uniqueItems": true,
        "items": {
          "$ref": "#/components/schemas/EvidenceSupportInput"
        }
      }
    }
  },
  "GateInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "name",
      "kind",
      "hard",
      "independence_required",
      "required_evidence"
    ],
    "properties": {
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "kind": {
        "type": "string",
        "enum": [
          "automated",
          "review",
          "approval"
        ]
      },
      "hard": {
        "type": "boolean"
      },
      "independence_required": {
        "type": "boolean"
      },
      "required_evidence": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "uniqueItems": true,
        "items": {
          "$ref": "#/components/schemas/EvidenceRequirementInput"
        }
      }
    }
  },
  "VerdictInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "result",
      "evidence_ids",
      "findings"
    ],
    "properties": {
      "result": {
        "type": "string",
        "enum": [
          "pass",
          "fail"
        ]
      },
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "maxItems": 64,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "findings": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "$ref": "#/components/schemas/FindingInput"
        }
      }
    }
  },
  "FindingDispositionInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "evidence_ids",
      "rationale"
    ],
    "properties": {
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "maxItems": 64,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "SubmissionInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "evidence_ids",
      "note"
    ],
    "properties": {
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "maxItems": 64,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "note": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "BounceInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "finding_id",
      "rationale"
    ],
    "properties": {
      "finding_id": {
        "type": "string",
        "format": "uuid"
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "ReviewNoteInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "note"
    ],
    "properties": {
      "note": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "WaiverInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "question",
      "choice",
      "alternatives",
      "rationale",
      "evidence_ids",
      "consequences",
      "residual_risk"
    ],
    "properties": {
      "question": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "choice": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "alternatives": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 1000
        }
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "maxItems": 64,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "consequences": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      },
      "residual_risk": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  }
} as const;
const componentSchemas = {
  "LoginRequest": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "email",
      "password"
    ],
    "properties": {
      "email": {
        "type": "string",
        "format": "email",
        "minLength": 1,
        "maxLength": 320
      },
      "password": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1024
      }
    }
  },
  "Session": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "actor_id",
      "actor_kind",
      "expires_at",
      "csrf_token"
    ],
    "properties": {
      "actor_id": {
        "type": "string",
        "format": "uuid"
      },
      "actor_kind": {
        "type": "string",
        "enum": [
          "human"
        ]
      },
      "expires_at": {
        "type": "string",
        "format": "date-time"
      },
      "csrf_token": {
        "type": "string",
        "minLength": 32,
        "maxLength": 256,
        "description": "Submit as X-CSRF-Token with the issued session cookie on protected mutations."
      }
    }
  },
  "CreateExplorationProject": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "title",
      "outcome",
      "hypothesis",
      "falsifier",
      "decision_criteria",
      "experiment_bound"
    ],
    "properties": {
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "outcome": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "hypothesis": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "falsifier": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "decision_criteria": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500,
          "pattern": "\\S"
        }
      },
      "experiment_bound": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1000,
        "pattern": "\\S"
      }
    }
  },
  "Project": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "title",
      "outcome",
      "mode",
      "state",
      "version",
      "hypothesis",
      "falsifier",
      "decision_criteria",
      "experiment_bound"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "outcome": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "mode": {
        "type": "string",
        "enum": [
          "exploration",
          "exploitation"
        ]
      },
      "state": {
        "type": "string",
        "enum": [
          "proposed",
          "active",
          "held",
          "completed",
          "stopped",
          "cancelled"
        ]
      },
      "version": {
        "type": "integer",
        "minimum": 1
      },
      "hypothesis": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "falsifier": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "decision_criteria": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500,
          "pattern": "\\S"
        }
      },
      "experiment_bound": {
        "type": "string",
        "minLength": 1,
        "maxLength": 1000,
        "pattern": "\\S"
      }
    }
  },
  "RecordDecision": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "kind",
      "question",
      "choice",
      "alternatives",
      "rationale",
      "evidence",
      "consequences"
    ],
    "properties": {
      "kind": {
        "type": "string",
        "enum": [
          "continue"
        ]
      },
      "question": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "choice": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "alternatives": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 1000
        }
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "evidence": {
        "type": "array",
        "maxItems": 64,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      },
      "consequences": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      }
    }
  },
  "LifecycleReasonRequest": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "reason"
    ],
    "properties": {
      "reason": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "DeliverableInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "title",
      "description",
      "required",
      "weight",
      "state",
      "acceptance_criteria"
    ],
    "properties": {
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "description": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "required": {
        "type": "boolean"
      },
      "weight": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1000
      },
      "state": {
        "type": "string",
        "enum": [
          "draft",
          "ready"
        ]
      },
      "acceptance_criteria": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000,
          "pattern": "\\S"
        }
      }
    }
  },
  "EvidenceSupportInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "target_type",
      "target_id"
    ],
    "properties": {
      "target_type": {
        "type": "string",
        "enum": [
          "deliverable",
          "gate",
          "finding",
          "decision"
        ]
      },
      "target_id": {
        "type": "string",
        "format": "uuid"
      }
    }
  },
  "EvidenceInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "kind",
      "title",
      "claim",
      "source",
      "metadata",
      "supports"
    ],
    "properties": {
      "kind": {
        "type": "string",
        "enum": [
          "report",
          "test-run",
          "link",
          "image",
          "observation",
          "measurement"
        ]
      },
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "claim": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "source": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "content": {
        "type": "string",
        "minLength": 1,
        "maxLength": 16000,
        "pattern": "\\S"
      },
      "uri": {
        "type": "string",
        "format": "uri-reference",
        "minLength": 1,
        "maxLength": 2000
      },
      "metadata": {
        "type": "object",
        "maxProperties": 32,
        "additionalProperties": {
          "type": "string",
          "minLength": 1,
          "maxLength": 2000
        }
      },
      "supports": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "uniqueItems": true,
        "items": {
          "$ref": "#/components/schemas/EvidenceSupportInput"
        }
      }
    }
  },
  "EvidenceRequirementInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "kind",
      "claim"
    ],
    "properties": {
      "kind": {
        "type": "string",
        "enum": [
          "report",
          "test-run",
          "link",
          "image",
          "observation",
          "measurement"
        ]
      },
      "claim": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      }
    }
  },
  "GateInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "name",
      "kind",
      "hard",
      "independence_required",
      "required_evidence"
    ],
    "properties": {
      "name": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "kind": {
        "type": "string",
        "enum": [
          "automated",
          "review",
          "approval"
        ]
      },
      "hard": {
        "type": "boolean"
      },
      "independence_required": {
        "type": "boolean"
      },
      "required_evidence": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "uniqueItems": true,
        "items": {
          "$ref": "#/components/schemas/EvidenceRequirementInput"
        }
      }
    }
  },
  "FindingInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "title",
      "detail",
      "blocking"
    ],
    "properties": {
      "title": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200,
        "pattern": "\\S"
      },
      "detail": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "blocking": {
        "type": "boolean"
      }
    }
  },
  "VerdictInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "result",
      "evidence_ids",
      "findings"
    ],
    "properties": {
      "result": {
        "type": "string",
        "enum": [
          "pass",
          "fail"
        ]
      },
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "maxItems": 64,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "findings": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "$ref": "#/components/schemas/FindingInput"
        }
      }
    }
  },
  "FindingDispositionInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "evidence_ids",
      "rationale"
    ],
    "properties": {
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "maxItems": 64,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "SubmissionInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "evidence_ids",
      "note"
    ],
    "properties": {
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "maxItems": 64,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "note": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "BounceInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "finding_id",
      "rationale"
    ],
    "properties": {
      "finding_id": {
        "type": "string",
        "format": "uuid"
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "ReviewNoteInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "note"
    ],
    "properties": {
      "note": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "WaiverInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "question",
      "choice",
      "alternatives",
      "rationale",
      "evidence_ids",
      "consequences",
      "residual_risk"
    ],
    "properties": {
      "question": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "choice": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "alternatives": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 1000
        }
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "maxItems": 64,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "consequences": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      },
      "residual_risk": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "PromotionDecision": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "question",
      "choice",
      "alternatives",
      "rationale",
      "evidence",
      "consequences"
    ],
    "properties": {
      "question": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "choice": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "alternatives": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 1000
        }
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "evidence": {
        "type": "array",
        "maxItems": 64,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      },
      "consequences": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      }
    }
  },
  "PromoteProjectRequest": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "decision",
      "residual_uncertainty",
      "priority_rationale",
      "deliverables"
    ],
    "properties": {
      "decision": {
        "$ref": "#/components/schemas/PromotionDecision"
      },
      "residual_uncertainty": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "priority_rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "deliverables": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "$ref": "#/components/schemas/DeliverableInput"
        }
      }
    }
  },
  "ForecastInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "p50_at",
      "p90_at",
      "review_after",
      "basis",
      "assumptions",
      "reason_codes",
      "impact"
    ],
    "properties": {
      "p50_at": {
        "type": "string",
        "format": "date-time"
      },
      "p90_at": {
        "type": "string",
        "format": "date-time"
      },
      "review_after": {
        "type": "string",
        "format": "date-time"
      },
      "basis": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "assumptions": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000,
          "pattern": "\\S"
        }
      },
      "reason_codes": {
        "type": "array",
        "minItems": 1,
        "maxItems": 9,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "enum": [
            "scope-change",
            "new-evidence",
            "dependency-change",
            "capacity-change",
            "quality-finding",
            "incident",
            "estimate-correction",
            "hold-change",
            "other"
          ]
        }
      },
      "impact": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "TargetInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "target_at",
      "reason"
    ],
    "properties": {
      "target_at": {
        "type": "string",
        "format": "date-time"
      },
      "reason": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "DeadlineInput": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "deadline_at",
      "source",
      "description"
    ],
    "properties": {
      "deadline_at": {
        "type": "string",
        "format": "date-time"
      },
      "source": {
        "type": "string",
        "enum": [
          "contract",
          "launch-window",
          "demonstration",
          "regulation",
          "other"
        ]
      },
      "description": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      }
    }
  },
  "Decision": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "project_id",
      "actor_id",
      "actor_kind",
      "principal_id",
      "recorded_at",
      "kind",
      "question",
      "choice",
      "alternatives",
      "rationale",
      "evidence",
      "consequences"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "actor_id": {
        "type": "string",
        "format": "uuid"
      },
      "actor_kind": {
        "type": "string",
        "enum": [
          "human",
          "agent"
        ]
      },
      "principal_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "recorded_at": {
        "type": "string",
        "format": "date-time"
      },
      "kind": {
        "type": "string",
        "enum": [
          "continue",
          "promote",
          "waiver"
        ]
      },
      "question": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "choice": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2000,
        "pattern": "\\S"
      },
      "alternatives": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 1000
        }
      },
      "rationale": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4000,
        "pattern": "\\S"
      },
      "evidence": {
        "type": "array",
        "maxItems": 64,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      },
      "consequences": {
        "type": "array",
        "maxItems": 32,
        "items": {
          "type": "string",
          "maxLength": 2000
        }
      }
    }
  },
  "Deliverable": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "title",
      "description",
      "required",
      "weight",
      "state",
      "acceptance_criteria",
      "version",
      "waiver_decision_id",
      "created_by",
      "created_at",
      "updated_at"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "title": {
        "type": "string"
      },
      "description": {
        "type": "string"
      },
      "required": {
        "type": "boolean"
      },
      "weight": {
        "type": "integer"
      },
      "state": {
        "type": "string",
        "enum": [
          "draft",
          "ready",
          "submitted",
          "bounced",
          "accepted",
          "waived",
          "cancelled"
        ]
      },
      "acceptance_criteria": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "version": {
        "type": "integer",
        "minimum": 1
      },
      "waiver_decision_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "created_by": {
        "type": "string",
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      },
      "updated_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  },
  "EvidenceSupport": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "target_type",
      "target_id"
    ],
    "properties": {
      "target_type": {
        "type": "string",
        "enum": [
          "deliverable",
          "gate",
          "finding",
          "decision"
        ]
      },
      "target_id": {
        "type": "string",
        "format": "uuid"
      }
    }
  },
  "Evidence": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "kind",
      "title",
      "claim",
      "source",
      "content",
      "uri",
      "metadata",
      "integrity_digest",
      "produced_by",
      "producer_kind",
      "principal_id",
      "produced_at",
      "supersedes_id",
      "supports"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "kind": {
        "type": "string",
        "enum": [
          "report",
          "test-run",
          "link",
          "image",
          "observation",
          "measurement"
        ]
      },
      "title": {
        "type": "string"
      },
      "claim": {
        "type": "string"
      },
      "source": {
        "type": "string"
      },
      "content": {
        "type": [
          "string",
          "null"
        ]
      },
      "uri": {
        "type": [
          "string",
          "null"
        ],
        "format": "uri-reference"
      },
      "metadata": {
        "type": "object",
        "additionalProperties": {
          "type": "string"
        }
      },
      "integrity_digest": {
        "type": "string",
        "pattern": "^[0-9a-f]{64}$"
      },
      "produced_by": {
        "type": "string",
        "format": "uuid"
      },
      "producer_kind": {
        "type": "string",
        "enum": [
          "human",
          "agent"
        ]
      },
      "principal_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "produced_at": {
        "type": "string",
        "format": "date-time"
      },
      "supersedes_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "supports": {
        "type": "array",
        "minItems": 1,
        "items": {
          "$ref": "#/components/schemas/EvidenceSupport"
        }
      }
    }
  },
  "Gate": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "deliverable_id",
      "name",
      "kind",
      "hard",
      "independence_required",
      "state",
      "version",
      "waiver_decision_id",
      "required_evidence",
      "created_by",
      "created_at",
      "updated_at"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "deliverable_id": {
        "type": "string",
        "format": "uuid"
      },
      "name": {
        "type": "string"
      },
      "kind": {
        "type": "string",
        "enum": [
          "automated",
          "review",
          "approval"
        ]
      },
      "hard": {
        "type": "boolean"
      },
      "independence_required": {
        "type": "boolean"
      },
      "state": {
        "type": "string",
        "enum": [
          "pending",
          "passed",
          "failed",
          "waived"
        ]
      },
      "version": {
        "type": "integer",
        "minimum": 1
      },
      "waiver_decision_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "required_evidence": {
        "type": "array",
        "minItems": 1,
        "items": {
          "$ref": "#/components/schemas/EvidenceRequirementInput"
        }
      },
      "created_by": {
        "type": "string",
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      },
      "updated_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  },
  "Verdict": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "deliverable_id",
      "gate_id",
      "result",
      "evidence_ids",
      "reviewer_id",
      "reviewer_kind",
      "principal_id",
      "supersedes_id",
      "created_at"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "deliverable_id": {
        "type": "string",
        "format": "uuid"
      },
      "gate_id": {
        "type": "string",
        "format": "uuid"
      },
      "result": {
        "type": "string",
        "enum": [
          "pass",
          "fail"
        ]
      },
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "reviewer_id": {
        "type": "string",
        "format": "uuid"
      },
      "reviewer_kind": {
        "type": "string",
        "enum": [
          "human",
          "agent"
        ]
      },
      "principal_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "supersedes_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  },
  "Finding": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "deliverable_id",
      "gate_id",
      "verdict_id",
      "title",
      "detail",
      "blocking",
      "state",
      "version",
      "created_by",
      "created_at",
      "updated_at"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "deliverable_id": {
        "type": "string",
        "format": "uuid"
      },
      "gate_id": {
        "type": "string",
        "format": "uuid"
      },
      "verdict_id": {
        "type": "string",
        "format": "uuid"
      },
      "title": {
        "type": "string"
      },
      "detail": {
        "type": "string"
      },
      "blocking": {
        "type": "boolean"
      },
      "state": {
        "type": "string",
        "enum": [
          "open",
          "resolved",
          "withdrawn"
        ]
      },
      "version": {
        "type": "integer",
        "minimum": 1
      },
      "created_by": {
        "type": "string",
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      },
      "updated_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  },
  "FindingAction": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "deliverable_id",
      "finding_id",
      "action",
      "evidence_ids",
      "rationale",
      "actor_id",
      "actor_kind",
      "principal_id",
      "created_at"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "deliverable_id": {
        "type": "string",
        "format": "uuid"
      },
      "finding_id": {
        "type": "string",
        "format": "uuid"
      },
      "action": {
        "type": "string",
        "enum": [
          "resolve",
          "withdraw"
        ]
      },
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "rationale": {
        "type": "string"
      },
      "actor_id": {
        "type": "string",
        "format": "uuid"
      },
      "actor_kind": {
        "type": "string",
        "enum": [
          "human",
          "agent"
        ]
      },
      "principal_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  },
  "Submission": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "deliverable_id",
      "kind",
      "evidence_ids",
      "note",
      "submitted_by",
      "submitter_kind",
      "principal_id",
      "created_at"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "deliverable_id": {
        "type": "string",
        "format": "uuid"
      },
      "kind": {
        "type": "string",
        "enum": [
          "submit",
          "resubmit"
        ]
      },
      "evidence_ids": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "string",
          "format": "uuid"
        }
      },
      "note": {
        "type": "string"
      },
      "submitted_by": {
        "type": "string",
        "format": "uuid"
      },
      "submitter_kind": {
        "type": "string",
        "enum": [
          "human",
          "agent"
        ]
      },
      "principal_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  },
  "VerdictResult": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "gate",
      "verdict",
      "findings"
    ],
    "properties": {
      "gate": {
        "$ref": "#/components/schemas/Gate"
      },
      "verdict": {
        "$ref": "#/components/schemas/Verdict"
      },
      "findings": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/Finding"
        }
      }
    }
  },
  "FindingActionResult": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "finding",
      "action"
    ],
    "properties": {
      "finding": {
        "$ref": "#/components/schemas/Finding"
      },
      "action": {
        "$ref": "#/components/schemas/FindingAction"
      }
    }
  },
  "SubmissionResult": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "deliverable",
      "submission"
    ],
    "properties": {
      "deliverable": {
        "$ref": "#/components/schemas/Deliverable"
      },
      "submission": {
        "$ref": "#/components/schemas/Submission"
      }
    }
  },
  "DeliverableWaiverResult": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "deliverable",
      "decision"
    ],
    "properties": {
      "deliverable": {
        "$ref": "#/components/schemas/Deliverable"
      },
      "decision": {
        "$ref": "#/components/schemas/Decision"
      }
    }
  },
  "GateWaiverResult": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "gate",
      "decision"
    ],
    "properties": {
      "gate": {
        "$ref": "#/components/schemas/Gate"
      },
      "decision": {
        "$ref": "#/components/schemas/Decision"
      }
    }
  },
  "Forecast": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "scope",
      "p50_at",
      "p90_at",
      "review_after",
      "basis",
      "assumptions",
      "reason_codes",
      "impact",
      "created_by",
      "created_at",
      "current",
      "stale",
      "attention_only"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "deliverable_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "scope": {
        "type": "string",
        "enum": [
          "project",
          "deliverable"
        ]
      },
      "p50_at": {
        "type": "string",
        "format": "date-time"
      },
      "p90_at": {
        "type": "string",
        "format": "date-time"
      },
      "review_after": {
        "type": "string",
        "format": "date-time"
      },
      "basis": {
        "type": "string"
      },
      "assumptions": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "reason_codes": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "impact": {
        "type": "string"
      },
      "supersedes_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "created_by": {
        "type": "string",
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      },
      "current": {
        "type": "boolean"
      },
      "stale": {
        "type": "boolean"
      },
      "attention_only": {
        "type": "boolean",
        "const": true
      }
    }
  },
  "Target": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "target_at",
      "reason",
      "created_by",
      "created_at",
      "current",
      "missed",
      "attention_only"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "target_at": {
        "type": "string",
        "format": "date-time"
      },
      "reason": {
        "type": "string"
      },
      "supersedes_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "created_by": {
        "type": "string",
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      },
      "current": {
        "type": "boolean"
      },
      "missed": {
        "type": "boolean"
      },
      "attention_only": {
        "type": "boolean",
        "const": true
      }
    }
  },
  "Deadline": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "id",
      "organization_id",
      "project_id",
      "deadline_at",
      "source",
      "description",
      "created_by",
      "created_at",
      "current",
      "passed",
      "attention_only"
    ],
    "properties": {
      "id": {
        "type": "string",
        "format": "uuid"
      },
      "organization_id": {
        "type": "string",
        "format": "uuid"
      },
      "project_id": {
        "type": "string",
        "format": "uuid"
      },
      "deadline_at": {
        "type": "string",
        "format": "date-time"
      },
      "source": {
        "type": "string",
        "enum": [
          "contract",
          "launch-window",
          "demonstration",
          "regulation",
          "other"
        ]
      },
      "description": {
        "type": "string"
      },
      "supersedes_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "created_by": {
        "type": "string",
        "format": "uuid"
      },
      "created_at": {
        "type": "string",
        "format": "date-time"
      },
      "current": {
        "type": "boolean"
      },
      "passed": {
        "type": "boolean"
      },
      "attention_only": {
        "type": "boolean",
        "const": true
      }
    }
  },
  "PromotionResult": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "project",
      "decision",
      "deliverables"
    ],
    "properties": {
      "project": {
        "$ref": "#/components/schemas/Project"
      },
      "decision": {
        "$ref": "#/components/schemas/Decision"
      },
      "deliverables": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/Deliverable"
        }
      }
    }
  },
  "VersionETag": {
    "type": "string",
    "pattern": "^\"[1-9][0-9]*\"$",
    "examples": [
      "\"7\""
    ]
  },
  "Activity": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "event_id",
      "event_type",
      "actor_id",
      "actor_kind",
      "principal_id",
      "aggregate_version",
      "command_id",
      "request_id",
      "occurred_at"
    ],
    "properties": {
      "event_id": {
        "type": "string",
        "format": "uuid"
      },
      "event_type": {
        "type": "string"
      },
      "actor_id": {
        "type": "string",
        "format": "uuid"
      },
      "actor_kind": {
        "type": "string",
        "enum": [
          "human",
          "agent"
        ]
      },
      "principal_id": {
        "type": [
          "string",
          "null"
        ],
        "format": "uuid"
      },
      "aggregate_version": {
        "type": "integer",
        "minimum": 1
      },
      "command_id": {
        "type": "string",
        "format": "uuid"
      },
      "request_id": {
        "type": "string"
      },
      "occurred_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  },
  "Problem": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "type",
      "title",
      "status",
      "code",
      "request_id"
    ],
    "properties": {
      "type": {
        "type": "string",
        "format": "uri-reference"
      },
      "title": {
        "type": "string"
      },
      "status": {
        "type": "integer",
        "minimum": 400,
        "maximum": 599
      },
      "code": {
        "type": "string"
      },
      "request_id": {
        "type": "string"
      },
      "detail": {
        "type": "string"
      }
    }
  },
  "SnapshotProblem": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "type",
      "title",
      "status",
      "code",
      "request_id",
      "detail",
      "links"
    ],
    "properties": {
      "type": {
        "type": "string",
        "format": "uri-reference"
      },
      "title": {
        "type": "string"
      },
      "status": {
        "const": 409
      },
      "code": {
        "const": "snapshot_required"
      },
      "request_id": {
        "type": "string"
      },
      "detail": {
        "type": "string"
      },
      "links": {
        "type": "object",
        "additionalProperties": {
          "type": "string"
        },
        "required": [
          "projects",
          "activity"
        ]
      }
    }
  }
} as const;
const requestContractParameters = {
  "IdempotencyKey": {
    "type": "string",
    "minLength": 16,
    "maxLength": 128
  },
  "ExpectedVersion": {
    "type": "string",
    "pattern": "^\"[1-9][0-9]*\"$",
    "examples": [
      "\"7\""
    ]
  }
} as const;
type RequestContractSchemaName = keyof typeof requestContractSchemas;
type RequestContractParameterName = keyof typeof requestContractParameters;

export const operationIds = [
  "activateProject",
  "approveDeliverable",
  "bounceDeliverable",
  "createDeliverable",
  "createEvidence",
  "createGate",
  "createProject",
  "getDeliverable",
  "getProject",
  "holdProject",
  "listDeliverableForecasts",
  "listDeliverables",
  "listEvidence",
  "listFindings",
  "listGates",
  "listProjectActivity",
  "listProjectForecasts",
  "listVerdicts",
  "login",
  "promoteProject",
  "recordDecision",
  "recordVerdict",
  "reforecastDeliverable",
  "reforecastProject",
  "resolveFinding",
  "resubmitDeliverable",
  "resumeProject",
  "reviseDeliverable",
  "setProjectDeadline",
  "setProjectTarget",
  "submitDeliverable",
  "supersedeEvidence",
  "waiveDeliverable",
  "waiveGate",
  "withdrawFinding"
] as const;
export type OperationId = (typeof operationIds)[number];

export class WorkplaneClient {
  constructor(
    private readonly baseUrl: string,
    private readonly fetcher: typeof fetch = globalThis.fetch.bind(globalThis),
  ) {}

  async activateProject(params: { project_id: string }, body: LifecycleReasonRequest, options: VersionedMutationOptions): Promise<VersionedResponse<Project>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/activate`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("LifecycleReasonRequest", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("activateProject response omitted a valid ETag");
    }
    return { body: response.body as Project, version: version as VersionETag };
  }
  async approveDeliverable(params: { id: string }, body: ReviewNoteInput, options: VersionedMutationOptions): Promise<VersionedResponse<Deliverable>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}/approve`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("ReviewNoteInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("approveDeliverable response omitted a valid ETag");
    }
    return { body: response.body as Deliverable, version: version as VersionETag };
  }
  async bounceDeliverable(params: { id: string }, body: BounceInput, options: VersionedMutationOptions): Promise<VersionedResponse<Deliverable>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}/bounce`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("BounceInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("bounceDeliverable response omitted a valid ETag");
    }
    return { body: response.body as Deliverable, version: version as VersionETag };
  }
  async createDeliverable(params: { project_id: string }, body: DeliverableInput, options: VersionedMutationOptions): Promise<VersionedResponse<Deliverable>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/deliverables`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("DeliverableInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("createDeliverable response omitted a valid ETag");
    }
    return { body: response.body as Deliverable, version: version as VersionETag };
  }
  async createEvidence(params: { project_id: string }, body: EvidenceInput, options: VersionedMutationOptions): Promise<VersionedResponse<Evidence>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/evidence`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("EvidenceInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("createEvidence response omitted a valid ETag");
    }
    return { body: response.body as Evidence, version: version as VersionETag };
  }
  async createGate(params: { id: string }, body: GateInput, options: VersionedMutationOptions): Promise<VersionedResponse<Gate>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}/gates`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("GateInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("createGate response omitted a valid ETag");
    }
    return { body: response.body as Gate, version: version as VersionETag };
  }
  async createProject(params: { org_id: string }, body: CreateExplorationProject, options: MutationOptions): Promise<Project> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    return (await this.request(`/api/v1/orgs/${encodeURIComponent(params.org_id)}/projects`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("CreateExplorationProject", body),
    })).body as Project;
  }
  async getDeliverable(params: { id: string }, options: AuthenticatedOptions = {}): Promise<Deliverable> {
    return (await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Deliverable;
  }
  async getProject(params: { project_id: string }, options: AuthenticatedOptions = {}): Promise<Project> {
    return (await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Project;
  }
  async holdProject(params: { project_id: string }, body: LifecycleReasonRequest, options: VersionedMutationOptions): Promise<VersionedResponse<Project>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/hold`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("LifecycleReasonRequest", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("holdProject response omitted a valid ETag");
    }
    return { body: response.body as Project, version: version as VersionETag };
  }
  async listDeliverableForecasts(params: { deliverable_id: string }, options: AuthenticatedOptions = {}): Promise<Array<Forecast>> {
    return (await this.request(`/api/v1/deliverables/${encodeURIComponent(params.deliverable_id)}/forecasts`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Array<Forecast>;
  }
  async listDeliverables(params: { project_id: string }, options: AuthenticatedOptions = {}): Promise<Array<Deliverable>> {
    return (await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/deliverables`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Array<Deliverable>;
  }
  async listEvidence(params: { project_id: string }, options: AuthenticatedOptions = {}): Promise<Array<Evidence>> {
    return (await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/evidence`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Array<Evidence>;
  }
  async listFindings(params: { id: string }, options: AuthenticatedOptions = {}): Promise<Array<Finding>> {
    return (await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}/findings`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Array<Finding>;
  }
  async listGates(params: { id: string }, options: AuthenticatedOptions = {}): Promise<Array<Gate>> {
    return (await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}/gates`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Array<Gate>;
  }
  async listProjectActivity(params: { project_id: string }, options: AuthenticatedOptions = {}): Promise<Array<Activity>> {
    return (await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/activity`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Array<Activity>;
  }
  async listProjectForecasts(params: { project_id: string }, options: AuthenticatedOptions = {}): Promise<Array<Forecast>> {
    return (await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/forecasts`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Array<Forecast>;
  }
  async listVerdicts(params: { gate_id: string }, options: AuthenticatedOptions = {}): Promise<Array<Verdict>> {
    return (await this.request(`/api/v1/gates/${encodeURIComponent(params.gate_id)}/verdicts`, {
      method: "GET",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    })).body as Array<Verdict>;
  }
  async login(params: Record<string, never>, body: LoginRequest, options: RequestOptions = {}): Promise<Session> {
    return (await this.request(`/api/v1/session/login`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("LoginRequest", body),
    })).body as Session;
  }
  async promoteProject(params: { project_id: string }, body: PromoteProjectRequest, options: VersionedMutationOptions): Promise<VersionedResponse<PromotionResult>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/promote`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("PromoteProjectRequest", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("promoteProject response omitted a valid ETag");
    }
    return { body: response.body as PromotionResult, version: version as VersionETag };
  }
  async recordDecision(params: { project_id: string }, body: RecordDecision, options: VersionedMutationOptions): Promise<VersionedResponse<Decision>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/decisions`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("RecordDecision", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("recordDecision response omitted a valid ETag");
    }
    return { body: response.body as Decision, version: version as VersionETag };
  }
  async recordVerdict(params: { gate_id: string }, body: VerdictInput, options: VersionedMutationOptions): Promise<VersionedResponse<VerdictResult>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/gates/${encodeURIComponent(params.gate_id)}/verdicts`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("VerdictInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("recordVerdict response omitted a valid ETag");
    }
    return { body: response.body as VerdictResult, version: version as VersionETag };
  }
  async reforecastDeliverable(params: { deliverable_id: string }, body: ForecastInput, options: VersionedMutationOptions): Promise<VersionedResponse<Forecast>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/deliverables/${encodeURIComponent(params.deliverable_id)}/forecasts`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("ForecastInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("reforecastDeliverable response omitted a valid ETag");
    }
    return { body: response.body as Forecast, version: version as VersionETag };
  }
  async reforecastProject(params: { project_id: string }, body: ForecastInput, options: VersionedMutationOptions): Promise<VersionedResponse<Forecast>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/forecasts`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("ForecastInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("reforecastProject response omitted a valid ETag");
    }
    return { body: response.body as Forecast, version: version as VersionETag };
  }
  async resolveFinding(params: { finding_id: string }, body: FindingDispositionInput, options: VersionedMutationOptions): Promise<VersionedResponse<FindingActionResult>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/findings/${encodeURIComponent(params.finding_id)}/resolve`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("FindingDispositionInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("resolveFinding response omitted a valid ETag");
    }
    return { body: response.body as FindingActionResult, version: version as VersionETag };
  }
  async resubmitDeliverable(params: { id: string }, body: SubmissionInput, options: VersionedMutationOptions): Promise<VersionedResponse<SubmissionResult>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}/resubmit`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("SubmissionInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("resubmitDeliverable response omitted a valid ETag");
    }
    return { body: response.body as SubmissionResult, version: version as VersionETag };
  }
  async resumeProject(params: { project_id: string }, body: LifecycleReasonRequest, options: VersionedMutationOptions): Promise<VersionedResponse<Project>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/resume`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("LifecycleReasonRequest", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("resumeProject response omitted a valid ETag");
    }
    return { body: response.body as Project, version: version as VersionETag };
  }
  async reviseDeliverable(params: { id: string }, body: DeliverableInput, options: VersionedMutationOptions): Promise<VersionedResponse<Deliverable>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}`, {
      method: "PATCH",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("DeliverableInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("reviseDeliverable response omitted a valid ETag");
    }
    return { body: response.body as Deliverable, version: version as VersionETag };
  }
  async setProjectDeadline(params: { project_id: string }, body: DeadlineInput, options: VersionedMutationOptions): Promise<VersionedResponse<Deadline>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/deadline`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("DeadlineInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("setProjectDeadline response omitted a valid ETag");
    }
    return { body: response.body as Deadline, version: version as VersionETag };
  }
  async setProjectTarget(params: { project_id: string }, body: TargetInput, options: VersionedMutationOptions): Promise<VersionedResponse<Target>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/projects/${encodeURIComponent(params.project_id)}/target`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("TargetInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("setProjectTarget response omitted a valid ETag");
    }
    return { body: response.body as Target, version: version as VersionETag };
  }
  async submitDeliverable(params: { id: string }, body: SubmissionInput, options: VersionedMutationOptions): Promise<VersionedResponse<SubmissionResult>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}/submit`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("SubmissionInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("submitDeliverable response omitted a valid ETag");
    }
    return { body: response.body as SubmissionResult, version: version as VersionETag };
  }
  async supersedeEvidence(params: { id: string }, body: EvidenceInput, options: VersionedMutationOptions): Promise<VersionedResponse<Evidence>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/evidence/${encodeURIComponent(params.id)}/supersede`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("EvidenceInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("supersedeEvidence response omitted a valid ETag");
    }
    return { body: response.body as Evidence, version: version as VersionETag };
  }
  async waiveDeliverable(params: { id: string }, body: WaiverInput, options: VersionedMutationOptions): Promise<VersionedResponse<DeliverableWaiverResult>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/deliverables/${encodeURIComponent(params.id)}/waive`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("WaiverInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("waiveDeliverable response omitted a valid ETag");
    }
    return { body: response.body as DeliverableWaiverResult, version: version as VersionETag };
  }
  async waiveGate(params: { gate_id: string }, body: WaiverInput, options: VersionedMutationOptions): Promise<VersionedResponse<GateWaiverResult>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/gates/${encodeURIComponent(params.gate_id)}/waive`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("WaiverInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("waiveGate response omitted a valid ETag");
    }
    return { body: response.body as GateWaiverResult, version: version as VersionETag };
  }
  async withdrawFinding(params: { finding_id: string }, body: FindingDispositionInput, options: VersionedMutationOptions): Promise<VersionedResponse<FindingActionResult>> {
    this.validateParameter("IdempotencyKey", options.idempotencyKey);
    this.validateParameter("ExpectedVersion", options.expectedVersion);
    const response = await this.request(`/api/v1/findings/${encodeURIComponent(params.finding_id)}/withdraw`, {
      method: "POST",
      headers: {
        ...this.extensionHeaders(options.headers),
      'Idempotency-Key': options.idempotencyKey,
      'If-Match': options.expectedVersion,
        ...this.securityHeaders(options.security),
      },
    body: this.validateRequestBody("FindingDispositionInput", body),
    });
    const version = response.headers.get("ETag");
    if (version === null || !versionETagPattern.test(version)) {
      throw new WorkplaneContractError("withdrawFinding response omitted a valid ETag");
    }
    return { body: response.body as FindingActionResult, version: version as VersionETag };
  }

  private async request(path: string, init: RequestInit): Promise<TransportResponse> {
    const response = await this.fetcher(`${this.baseUrl}${path}`, {
      ...init,
      credentials: "same-origin",
      headers: { "Content-Type": "application/json", ...init.headers },
    });
    const payload: unknown = await response.json();
    if (!response.ok) throw new WorkplaneProblem(response.status, payload);
    return { body: payload, headers: response.headers };
  }

  private validateRequestBody(schemaName: RequestContractSchemaName, body: unknown): string {
    this.validateSchema(requestContractSchemas[schemaName], body, schemaName);
    const encoded = JSON.stringify(body);
    if (encoded === undefined) {
      throw new WorkplaneContractError(`${schemaName} cannot be serialized as JSON`);
    }
    if (new TextEncoder().encode(encoded).byteLength > requestBodyMaxBytes) {
      throw new WorkplaneContractError(`${schemaName} exceeds the ${requestBodyMaxBytes} byte request limit`);
    }
    return encoded;
  }

  private validateParameter(name: RequestContractParameterName, value: unknown): void {
    this.validateSchema(requestContractParameters[name], value, name);
  }

  private validateSchema(schema: RuntimeSchema, value: unknown, path: string): void {
    if (schema.$ref !== undefined) {
      const name = schema.$ref.replace("#/components/schemas/", "") as keyof typeof componentSchemas;
      const resolved = componentSchemas[name];
      if (resolved === undefined) throw new WorkplaneContractError(`${path} references an unknown schema`);
      this.validateSchema(resolved, value, path);
      return;
    }
    if (schema.type === "object") {
      if (value === null || typeof value !== "object" || Array.isArray(value)) {
        throw new WorkplaneContractError(`${path} must be an object`);
      }
      const record = value as Record<string, unknown>;
      if (schema.maxProperties !== undefined && Object.keys(record).length > schema.maxProperties) {
        throw new WorkplaneContractError(`${path} must contain at most ${schema.maxProperties} properties`);
      }
      for (const required of schema.required ?? []) {
        if (!Object.hasOwn(record, required)) {
          throw new WorkplaneContractError(`${path}.${required} is required`);
        }
      }
      if (schema.additionalProperties === false) {
        const allowed = new Set(Object.keys(schema.properties ?? {}));
        const unknown = Object.keys(record).find((key) => !allowed.has(key));
        if (unknown !== undefined) {
          throw new WorkplaneContractError(`${path}.${unknown} is not declared by OpenAPI`);
        }
      }
      for (const [name, propertySchema] of Object.entries(schema.properties ?? {})) {
        if (Object.hasOwn(record, name)) this.validateSchema(propertySchema, record[name], `${path}.${name}`);
      }
      return;
    }
    if (schema.type === "array") {
      if (!Array.isArray(value)) throw new WorkplaneContractError(`${path} must be an array`);
      if (schema.minItems !== undefined && value.length < schema.minItems) {
        throw new WorkplaneContractError(`${path} must contain at least ${schema.minItems} items`);
      }
      if (schema.maxItems !== undefined && value.length > schema.maxItems) {
        throw new WorkplaneContractError(`${path} must contain at most ${schema.maxItems} items`);
      }
      if (schema.uniqueItems === true && new Set(value.map((item) => JSON.stringify(item))).size !== value.length) {
        throw new WorkplaneContractError(`${path} must contain unique items`);
      }
      if (schema.items !== undefined) {
        value.forEach((item, index) => this.validateSchema(schema.items!, item, `${path}[${index}]`));
      }
      return;
    }
    if (schema.type === "string") {
      if (typeof value !== "string") throw new WorkplaneContractError(`${path} must be a string`);
      const length = Array.from(value).length;
      if (schema.minLength !== undefined && length < schema.minLength) {
        throw new WorkplaneContractError(`${path} must contain at least ${schema.minLength} characters`);
      }
      if (schema.maxLength !== undefined && length > schema.maxLength) {
        throw new WorkplaneContractError(`${path} must contain at most ${schema.maxLength} characters`);
      }
      if (schema.enum !== undefined && !schema.enum.includes(value)) {
        throw new WorkplaneContractError(`${path} is not an allowed value`);
      }
      if (schema.pattern !== undefined && !new RegExp(schema.pattern, "u").test(value)) {
        throw new WorkplaneContractError(`${path} does not match the public contract`);
      }
      return;
    }
    if (schema.type === "integer") {
      if (typeof value !== "number" || !Number.isSafeInteger(value)) {
        throw new WorkplaneContractError(`${path} must be an integer`);
      }
      if (schema.minimum !== undefined && value < schema.minimum) {
        throw new WorkplaneContractError(`${path} must be at least ${schema.minimum}`);
      }
      if (schema.maximum !== undefined && value > schema.maximum) {
        throw new WorkplaneContractError(`${path} must be at most ${schema.maximum}`);
      }
      return;
    }
    if (schema.type === "boolean" && typeof value !== "boolean") {
      throw new WorkplaneContractError(`${path} must be a boolean`);
    }
  }

  private securityHeaders(security?: RequestSecurity): Record<string, string> {
    if (security?.kind === "agent-bearer") {
      return { Authorization: `Bearer ${security.bearerToken}` };
    }
    if (security?.kind === "human-session" && security.csrfToken !== undefined) {
      return { "X-CSRF-Token": security.csrfToken };
    }
    return {};
  }

  private extensionHeaders(headers?: Record<string, string>): Record<string, string> {
    return Object.fromEntries(
      Object.entries(headers ?? {}).filter(
        ([name]) => !contractOwnedRequestHeaders.has(name.toLowerCase()),
      ),
    );
  }
}

export class WorkplaneProblem extends Error {
  constructor(public readonly status: number, public readonly payload: unknown) {
    super(`Workplane request failed (${status})`);
  }
}

export class WorkplaneContractError extends Error {}
